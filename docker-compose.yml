version: '3.8'
services:
  jupyter:
    image: python:3.11
    ports:
      - "0.0.0.0:8889:8888"
    volumes:
      - ./:/work
    working_dir: /work
    command: bash -c "pip install jupyterlab jupyterlab-citation-manager && jupyter lab --ip=0.0.0.0 --allow-root --no-browser"
